// WAP in C to calculate average of three numbers.
#include <stdio.h>
int main() {
    float a = 10, b = 20, c = 30;
    float avg = (a + b + c) / 3;
    printf("Average = %.2f", avg);
    return 0;
}